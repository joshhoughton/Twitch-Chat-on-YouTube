{
  "name": "twitch-chat-on-youtube",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rolldown -c && node scripts/copy-static.mjs",
    "watch": "node scripts/copy-static.mjs && rolldown -c --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "prettier": "^3.3.3",
    "rolldown": "^1.2.3",
    "typescript": "^7.0.2"
  }
}
